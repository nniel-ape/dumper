services:
  dumper:
    build: .
    ports:
      - "8980:8080"
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  dumper-data:
