services:
  dumper:
    build: .
    container_name: dumper-local
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
      - HTTP_PORT=8080
    ports:
      - "${HTTP_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
